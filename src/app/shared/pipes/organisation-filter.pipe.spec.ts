import { OrganisationFilterPipe } from './organisation-filter.pipe';

describe('OrganisationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrganisationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
